services:
  - type: web
    name: billing-app-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # FIXED: app:app instead of appapp
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
